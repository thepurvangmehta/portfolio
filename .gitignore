.DS_Store
__pycache__/
# Local-only gate password for deploy (read by deploy step, never committed).
.gate_pw

# NDA / gated case-study SOURCE (plaintext) must never be committed to this
# public repo. The built site ships only AES-encrypted ciphertext. Add every
# gated content/*.json file here.
content/healthcare.json
content/communication-saas.json
# Legacy Framer source for the gated page (also plaintext). Read locally at
# build time for the page <head>, but must not be committed to this public repo.
src_healthcare.html
src_communication-saas.html
