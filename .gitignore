.DS_Store
__pycache__/
# Local-only gate password for deploy (read by deploy step, never committed).
.gate_pw
# Cached Worker URL for the access-request flow (not secret, just local cache).
.access_api_url

# NDA / gated case-study SOURCE (plaintext) must never be committed to this
# public repo while gated:true. Currently ungated (temporary), so tracked
# normally -- re-add here if these go back to gated:true.
